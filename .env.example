# Same FastAPI backend as HR Dashboard
VITE_API_BASE_URL=http://localhost:8000

# Where the "Enter ->" button on the module marketplace points for the HR module.
# TODO: replace plain redirect with real SSO handoff (issue-code/consume) once built.
VITE_HR_DASHBOARD_URL=http://localhost:5174
